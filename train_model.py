import tensorflow as tf
import os

# Load data and split to test / train images and labels

# Build model:
    # Build layers
    # Compile model
    # Train model (model.fit)
    # Test accuracy

# Save model in order to load into other programs
